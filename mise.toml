[tools]
python = "3.11"

[env]
_.python.venv = { path = ".venv", create = true }

[tasks.install]
description = "Install Python dependencies"
run = "python -m pip install -r requirements.txt"

[tasks.app]
description = "Run the app (pass CLI args after --)"
run = "python main.py"
