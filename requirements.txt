# File monitoring
watchdog

# TOML configuration parsing
tomli

# Data class utilities
dacite

# Web scraping and browser automation
selenium
webdriver-manager

# HTTP clients
httpx[http2]
h2
requests

# Date/time parsing
parsedatetime
tzdata

# Microsoft Office 365 integration
O365

# Environment variable management
python-dotenv

# Native config builder GUI (installed into the app's private virtual environment)
PySide6-Essentials>=6.8,<7

# Gmail IMAP 2FA retrieval uses Python stdlib (`imaplib`, `email`), so there is
# no pip package to add here.
